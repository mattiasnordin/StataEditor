Changes in version 0.4.0:

	*Dropdown completions menu for variables (in active dataset), functions and commands. Only completions for variables are active by default.
	*Expanded the settings file with options for completions.